// A seeded mechanical finding for the Sweep e2e. The scanner reports this file
// until the marker below is removed (a real fix); a suppression directive would
// clear the scan but trip the audit. See README.md for the token semantics.
export const alpha = 1; // NEEDS_FIX: replace the placeholder with the real value
