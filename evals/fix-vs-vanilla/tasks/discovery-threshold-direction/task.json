{
  "id": "discovery-threshold-direction",
  "split": "discovery",
  "provenance": "discovery-created",
  "tuning_used": true,
  "archetype": "weak-repro",
  "prompt": "Our latency monitor is configured to warn at 500 ms and go critical at 900 ms, but a 950 ms reading shows as 'ok' and nobody gets paged. Fix severityOf in src/alert-severity.mjs so readings past the critical threshold page the on-call. The regression command is `npm test`; it should fail before the fix and pass after the fix.",
  "checks": [
    { "id": "node-test", "argv": ["npm", "test"] },
    { "id": "descending-direction", "hidden": true, "argv": ["node", "tests/alert-severity.descending.test.mjs"] }
  ],
  "allowed_changed_files": ["src/alert-severity.mjs"]
}
