{
  "id": "heldout-drop-bay",
  "split": "held-out",
  "provenance": "held-out-created",
  "tuning_used": false,
  "archetype": "weak-repro",
  "prompt": "Our floor controller calls dropBay() from src/drop-bay.mjs to read the drop-off bay out of a robot's route plan. A plan with no hand-off marker, like \"PICK-4>QA-2\", should come back as an empty string, but dropBay echoes the whole plan back, and dispatch then tries to send the robot to a bay called \"PICK-4>QA-2\". Run `npm test`: it fails on this input before the fix and must pass after.",
  "checks": [
    { "id": "node-test", "argv": ["npm", "test"] },
    { "id": "bay-contract", "hidden": true, "argv": ["node", "tests/drop-bay.contract.test.mjs"] }
  ],
  "allowed_changed_files": ["src/drop-bay.mjs"]
}
