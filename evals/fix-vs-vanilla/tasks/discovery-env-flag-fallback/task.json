{
  "id": "discovery-env-flag-fallback",
  "split": "discovery",
  "provenance": "discovery-created",
  "tuning_used": true,
  "archetype": "weak-repro",
  "prompt": "We set FEATURE_EXPORT=1 in staging and the export button stayed hidden. Fix flagValue in src/env-flag.mjs so the usual on spellings like '1' and 'yes' enable the flag. The regression command is `npm test`; it should fail before the fix and pass after the fix.",
  "checks": [
    { "id": "node-test", "argv": ["npm", "test"] },
    { "id": "flag-off", "hidden": true, "argv": ["node", "tests/env-flag.off.test.mjs"] }
  ],
  "allowed_changed_files": ["src/env-flag.mjs"]
}
