{
  "id": "discovery-truncate-ellipsis",
  "split": "discovery",
  "provenance": "discovery-created",
  "tuning_used": true,
  "archetype": "adjacent-regression",
  "objective_passes_at_baseline": true,
  "prompt": "Long titles overflow the card layout. Update `truncate(str, max)` in src/truncate.mjs so a string longer than `max` is shortened to fit and ends with an ellipsis (…) so the reader can tell it was cut. The regression command is `npm test`; it should fail before the fix and pass after the fix.",
  "checks": [
    { "id": "node-test", "argv": ["npm", "test"] },
    { "id": "short-guard", "hidden": true, "argv": ["node", "tests/truncate.short.test.mjs"] }
  ],
  "allowed_changed_files": ["src/truncate.mjs"]
}
