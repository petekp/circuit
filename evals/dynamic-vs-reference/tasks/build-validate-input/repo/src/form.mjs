// TODO: validate submitted form fields. `name` and `email` are required and
// must be non-empty after trimming. Return { ok: true, errors: [] } when valid,
// or { ok: false, errors: [...] } listing each missing field id. The stub
// currently accepts everything.
export function validateForm(fields) {
  return { ok: true, errors: [] };
}
