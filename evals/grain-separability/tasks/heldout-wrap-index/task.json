{
  "id": "heldout-wrap-index",
  "split": "held-out",
  "provenance": "held-out-created",
  "tuning_used": false,
  "archetype": "weak-repro",
  "prompt": "Our slideshow lets people page forward past the last slide, and when they do the index runs off the end of the list instead of wrapping back to the start. Fix `wrapIndex(index, len)` in src/wrap.mjs so paging past the last slide wraps around. The regression command is `npm test`; it should fail before the fix and pass after the fix. Keep the change focused.",
  "checks": [
    { "id": "node-test", "argv": ["npm", "test"] },
    { "id": "wrap-negative", "hidden": true, "argv": ["node", "tests/wrap.negative.test.mjs"] }
  ],
  "allowed_changed_files": ["src/wrap.mjs"],
  "separability": {
    "co_change": 0,
    "coupling": 0,
    "cross_part_decision": 0,
    "independent_verifiability": 0,
    "sum": 0,
    "band": "separable"
  }
}
